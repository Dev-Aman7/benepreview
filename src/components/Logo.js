import React from "react";
// import { Link } from "react-router-dom";
// import classNames from "classnames";
const Logo = ({ versions, parentClass, full, text }) => {
	// const componentClass = `c-logo`;
	// const versionClass = versions
	// 	.map((el) => `${componentClass}--${el}`)
	// 	.join(" ");
	// const parent = `${parentClass}__${componentClass.replace("c-", "")}`;
	// const classes = classNames(componentClass, {
	// 	[versionClass]: versions,
	// 	[parent]: parentClass,
	// });

	// if (text) {
	// 	return (
	// 		<img src="/images/logo-text.png" className="c-logo__img" alt="bené" />
	// 	);
	// }
	return (
		<div></div>
		// <Link href="/">
		// <div>
		// 	{!full && (
		// 		<img src="/images/logo-new.png" className="c-logo__img" alt="bené" />
		// 	)}
		// 	{full && (
		// 		<img src="/images/logo.png" className="c-logo__img" alt="bené" />
		// 	)}
		// </div>
		// </Link>
	);
};

export default Logo;
